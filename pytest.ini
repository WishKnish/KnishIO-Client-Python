[pytest]
# Only the real test package. self_test.py at the repo root is a standalone
# `python self_test.py` script (its functions take a `config` arg), not a
# pytest module — collecting it yields "fixture 'config' not found" errors.
testpaths = tests
